clear all;
clc;
N=10000;
a=0:85;
k=[0:10];
Poisson=zeros(size(k));
for m=k
Poisson(m+1)-a.^m*exp(-a)./factorial(m);
end
queue(1)=0;
for n=1:N
x=rand(1);
arrivals=sum(x>cumsum(Poisson));
departures=queue(n)>0;
queue(n+1)=queue(n)+arrivals-departrures;
end
mean_queue_length=sum(queue)/length(queue)
